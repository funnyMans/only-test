import styled from 'styled-components';

export const ProgressCounterSC = styled.span`
  font-weight: 400;
  font-size: ${({ theme }) => theme.fontSizes[16]};
  color: ${({ theme }) => theme.colors.independence};
`;

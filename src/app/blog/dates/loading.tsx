import { FlexContainerSC } from '@/shared/ui/containers/FlexContainer';

const LoadingDates = () => {
  return (
    <FlexContainerSC $fullHeight $fullWidth>
      loading
    </FlexContainerSC>
  );
};

export default LoadingDates;
